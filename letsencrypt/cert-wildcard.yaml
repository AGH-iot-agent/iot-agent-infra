apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: iotag-wildcard
  namespace: istio-system
spec:
  secretName: iotag-wildcard-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.iotag-dev.com"